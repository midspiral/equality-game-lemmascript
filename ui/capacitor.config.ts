import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lemmascript.equality-game',
  appName: 'Equality Game',
  webDir: 'dist'
};

export default config;
